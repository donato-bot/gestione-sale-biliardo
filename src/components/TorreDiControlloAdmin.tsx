// All'interno della tabella sale.map, aggiungi questo link di emergenza protetta
<a 
  href={`/dashboard/${sala.id}`} 
  className="text-xs font-bold uppercase text-gray-500 hover:text-white"
>
  Accesso Diretto (SuperAdmin)
</a>